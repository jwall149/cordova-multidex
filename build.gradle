android {
    defaultConfig {
        // Enabling multidex support.
        multiDexEnabled true
    }
    configurations { 
        all*.exclude group: 'com.google.android', module: 'actions' 
    }
}

dependencies {
  compile 'com.android.support:multidex:1.0.0'
}
