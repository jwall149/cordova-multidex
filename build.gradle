android {
    defaultConfig {
        // Enabling multidex support.
        multiDexEnabled true
    }
    configurations {
        all*.exclude  module: 'google-play-services'
    }
}

dependencies {
  compile 'com.android.support:multidex:1.0.0'
}
